import React from 'react';
import TodoList from './components/todoList';

const App = () => {
    return (
        <div className="container">
          <TodoList />
        </div>
    );
};

export default App;